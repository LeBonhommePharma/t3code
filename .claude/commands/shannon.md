---
description: Shannon hub/gate status for the t3code bench overlay (fail-open unless SHANNON_GATE_LIVE=1)
---

```bash
node lp/bench/cli.mjs shannon
```

Do not launch docking. Live gate only when `SHANNON_GATE_LIVE=1`.

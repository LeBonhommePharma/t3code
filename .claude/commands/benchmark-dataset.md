---
description: List FlexAIDDS Astex/YAML registry, BENCHMARK_STANDARD, and admission_metrics_contract (read-only)
---

```bash
node lp/bench/cli.mjs dataset registry
```

2HR7 is canary-only (84 of canonical 85). Do not launch the 85-set. Registry YAMLs live in `benchmarks/datasets/`.

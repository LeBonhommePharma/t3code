---
description: Admit a FlexAIDDS bench arm if standdown allows (no 85-launch, no GA pb_clash)
argument-hint: "<arm-id>"
---

Admit only a known standdown-safe arm (`pool-native`, `claim-ready`, `canary-2hr7`).

```bash
node lp/bench/cli.mjs admit $ARGUMENTS
```
